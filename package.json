{
  "name": "careersheets_dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "(cd clients && npm start) & (cd server && npm run dev)",
    "startProduct":"(cd clients && npm run build) && (cd server && npm start)",
    "installDep":"(cd clients && npm install) && (cd server && npm install)",
    "onlyBackendProduct":"cd server && npm start",
    "onlyFrontendProduct":"cd clients && npm run build",
    "installBackendDep":"cd server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mui/base": "^5.0.0-beta.14"
  }
}
